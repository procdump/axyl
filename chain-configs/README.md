# Chain Configs

This directory contains the genesis and general configurations for testnet and mainnet.
These will be built into the rayls-network binary for use with the --chain command.

These are the live configs in use by `--chain testnet` and `--chain mainnet`. The
`testnet/` subdirectory contains `testnet_accounts.yaml`, the canonical prefund map
applied at testnet genesis.
